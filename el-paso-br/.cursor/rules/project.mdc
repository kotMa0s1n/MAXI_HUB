---
description: El Paso Border Roleplay hub
alwaysApply: true
---

# El Paso BR

Project root: `Z:\Desktop\i.s.m\Script\el-paso-br\`

| PlaceId | Game |
|---------|------|
| `14502598369` | El Paso, Texas: Border Roleplay |

## Files

| File | Role |
|------|------|
| `epbr-logic.lua` | Hub logic source |
| `el-paso-br.lua` | Bundled hub (UI + logic) — **run this** |
| `RUN.lua` | Potassium entry: `loadstring(readfile("el-paso-br/RUN.lua"))()` |
| `bundle-standalone.ps1` | Build `el-paso-br.lua` |
| `deploy-to-potassium.ps1` | Copy to Potassium workspace |

## UI

- Канон UI: копировать `maxi-hub\maxi-hub-ui.lua` в bundle, **не редактировать** в папке игры
- `guiName`: `MaxiHubEPBR`
- Hotkey: **RightShift**
- Вкладка **Кредиты** обязательна
- Габариты: `maxi-hub\.cursor\rules\ui-layout.mdc`

## Авто-выгрузка

При повторном запуске сначала вызывается `getgenv().EPBR_Stop()` — останавливает логику, ESP, потоки, noclip.

## Git (MAXI_HUB)

Git-репо: `Z:\Desktop\i.s.m\Script\MAXI_HUB\` → `kotMa0s1n/MAXI_HUB`, папка `el-paso-br/`.

**Перед любыми правками** — всегда pull:

```powershell
cd "Z:\Desktop\i.s.m\Script\MAXI_HUB"
git pull origin main
```

Редактировать исходники в `Script\el-paso-br\`, затем копировать в `MAXI_HUB\el-paso-br\`, commit + push.

## После правок

```powershell
powershell -File bundle-standalone.ps1
```

Сборка **сама** копирует файлы в Potassium workspace (`deploy-to-potassium.ps1` вызывается автоматически).
